# Set these according to where you want the data centered around.
# Below example data is for Seattle, WA + Bay Area, CA.
# Map will be initially centered on the first coordinate in the array.
LOCATIONS=(
    47.62,-122.13
    37.50,-122.04
)
SERVER_PORT=${SERVER_PORT:-8086}
ENABLE_GIT=${ENABLE_GIT:-1}
